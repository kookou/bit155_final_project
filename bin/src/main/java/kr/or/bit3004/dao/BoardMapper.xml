<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.bit3004.dao.BoardDao">
	<select id="getBoardList" resultType="kr.or.bit3004.dto.Board">
		select b.board_no, b.title, b.content, b.views, b.write_date, b.comment_count, b.refer, b.depth, b.step, b.no, b.id, u.NICKNAME
		  from board_list b
		 inner join `user` u
		    on b.id = u.id
		 where no=#{no}
	</select>
</mapper>